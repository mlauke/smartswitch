#
# docker run --rm -p 8080:80 --name nginx -v $(pwd):/usr/share/nginx/html:ro -v $(pwd)/nginx.conf:/etc/nginx/conf.d/default.conf:ro nginx

server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /api {
        #proxy_pass http://192.168.188.47;
        proxy_pass http://192.168.188.47;
    }

    location /update {
        proxy_pass http://192.168.188.47;
    }
}